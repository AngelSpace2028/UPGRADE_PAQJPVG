exec("""\n#!/usr/bin/env python3\nh='inf'\ng=b'\\x00'\nf=reversed\ne=enumerate\nd=ImportError\nZ=b'P'\nY=float\nX=str\nW=min\nV=all\nT=b'N'\nS=b'Z'\nR=open\nQ='.'\nP=''\nM=input\nN=Exception\nL=int\nK=bytearray\nI=b''\nH=bytes\nF=None\nE=False\nD=True\nC=range\nB=print\nA=len\nimport math,random as U,decimal as G\nfrom typing import Optional,List,Tuple,Dict,Callable\ntry:import paq as J\nexcept d:J=F\ntry:import zstandard as a;b=a.ZstdCompressor(level=22);c=a.ZstdDecompressor();O=D\nexcept d:O=E\ni='PAQJP_8.8_LOSSLESS_AUTO_SAFE'\nm=[A for A in C(2,256)if V(A%B!=0 for B in C(2,L(A**.5)+1))]\nj=[79,17,111]\ndef n(n):\n	E=0\n	while D:\n		A,B=n-E,n+E\n		if A>=2 and V(A%B!=0 for B in C(2,L(A**.5)+1)):return A\n		if B>=2 and V(B%A!=0 for A in C(2,L(B**.5)+1)):return B\n		E+=1\nclass k:\n	def __init__(A):A.PI_DIGITS=j.copy();A.seed_tables=A._gen_seed_tables(num=126,size=40,seed=42);A.fibonacci=A._gen_fib(100);A.PI_STR='3.14159265358979323846264338327950288419716939937510';A._build_transform_maps();A.sequences=A._build_pair_sequences();A.pair_lookup={A:(B,C)for(A,(B,C))in e(A.sequences)}\n	def get_pi_digits(A,n):\n		if n<1:return P\n		return A.PI_STR[2:2+n]\n	def find_lossless_k(C,n):\n		if n<1:return 0,D\n		J=C.get_pi_digits(n);E=L(C.PI_STR.replace(Q,P)[:n+1]);A=16777216;G.getcontext().prec=50;F=G.Decimal(C.PI_STR);H=(F-3)*A;B=L(round(H));B=max(0,W(B,A-1));I=(3*10**n*A+B*10**n)//A;return B,I==E\n	def to_bin(A,value,bits):return format(value,'b').zfill(bits)\n	def get_bit_size(A,k):return 23 if k<=8388607 else 25\n	def transform_17(E,data):\n		if not data:return I\n		G,P=E.find_lossless_k(7);O=E.get_bit_size(G);J=E.to_bin(G,O);M=[]\n		for B in C(0,A(J),8):\n			D=J[B:B+8]\n			if A(D)<8:D=D.ljust(8,'0')\n			M.append(L(D,2))\n		N=H(M);F=K(data)\n		for B in C(A(F)):F[B]^=N[B%A(N)]\n		return H(F)\n	reverse_transform_17=transform_17\n	def get_basel_digits(B,n):G.getcontext().prec=n+5;A=G.Decimal(B.PI_STR);C=A*A/G.Decimal(6);D=X(C).replace(Q,P);return D[:n]\n	def get_one_over_e_digits(D,n):G.getcontext().prec=n+5;A=G.Decimal(1).exp();B=G.Decimal(1)/A;C=X(B).replace(Q,P);return C[:n]\n	def get_5e_digits(D,n):G.getcontext().prec=n+5;A=G.Decimal(1).exp();B=G.Decimal(5)*A;C=X(B).replace(Q,P);return C[:n]\n	def _gen_seed_tables(A,num=126,size=40,seed=42):U.seed(seed);return[[U.randint(5,255)for A in C(size)]for A in C(num)]\n	def _gen_fib(E,n):\n		B,A=0,1;D=[B,A]\n		for F in C(2,n):B,A=A,B+A;D.append(A)\n		return D\n	def get_seed(B,idx,val):\n		if 0<=idx<A(B.seed_tables):return B.seed_tables[idx][val%40]\n		return 0\n	def _append_bits(B,bitlist,value,count):\n		for A in C(count-1,-1,-1):bitlist.append(value>>A&1)\n	def _read_bits(E,bits,pos,count):\n		B=0\n		for D in C(count):\n			if pos+D>=A(bits):return 0\n			B=B<<1|bits[pos+D]\n		return B\n	def transform_00(W,data):\n		E=data\n		if not E:return g\n		I=F;J=Y(h);L=[];X=10;M=K(E);R=[]\n		for a in C(X):\n			N=0;O=M;S=Y('-inf')\n			for T in C(256):\n				B=K(M)\n				for U in C(A(B)):B[U]=(B[U]+T)%256\n				P=0;D=0\n				while D<A(B):\n					Z=B[D];Q=1;D+=1\n					while D<A(B)and B[D]==Z:Q+=1;D+=1\n					P+=Q*Q\n				if P>S:S=P;O=B;N=T\n			R.append(N);G=W._apply_rle_to_shifted(O,N)\n			if A(G)<J:J=A(G);I=G;L=R.copy()\n			M=O\n			if A(G)>=A(E):break\n		if I is F or J>=A(E):return H([0])+E\n		V=K([A(L)]);V.extend(L);return V+I\n	def _apply_rle_to_shifted(D,shifted_data,shift):\n		I=shifted_data;B=[];D._append_bits(B,2,3);D._append_bits(B,shift,8);F=0;L=A(I)\n		while F<L:\n			G=I[F];E=1;F+=1\n			while F<L and I[F]==G:E+=1;F+=1\n			while E>=13:M=W(E,268);D._append_bits(B,15,4);D._append_bits(B,M-13,8);D._append_bits(B,G,8);E-=M\n			if E==1:D._append_bits(B,0,2);D._append_bits(B,G,8)\n			elif E<=5:D._append_bits(B,1,2);D._append_bits(B,E-2,2);D._append_bits(B,G,8)\n			elif E<=12:D._append_bits(B,2,2);D._append_bits(B,E-6,3);D._append_bits(B,G,8)\n		Q=(8-A(B)%8)%8;D._append_bits(B,0,Q);N=K()\n		for O in C(0,A(B),8):\n			J=0\n			for P in C(8):\n				if O+P<A(B):J=J<<1|B[O+P]\n			N.append(J)\n		return H(N)\n	def reverse_transform_00(L,cdata):\n		B=cdata\n		if not B or B==g:return I\n		if B[0]==0:return B[1:]\n		D=B[0]\n		if D==0 or A(B)<1+D:return I\n		M=list(B[1:1+D]);N=B[1+D:];G=L._rle_decode(N)\n		if G is F:return I\n		E=K(G)\n		for O in f(M):\n			for J in C(A(E)):E[J]=(E[J]-O)%256\n		return H(E)\n	def _rle_decode(F,data):\n		if not data:return\n		D=[]\n		for L in data:\n			for H in C(7,-1,-1):D.append(L>>H&1)\n		B=0;E=A(D)\n		if E<11:return\n		M=F._read_bits(D,B,3);B+=3\n		if M!=2:return\n		B+=8;J=K()\n		while B<E:\n			if B+2>E:break\n			I=F._read_bits(D,B,2);B+=2\n			if I==0:\n				if B+8>E:break\n				G=1\n			elif I==1:\n				if B+2+8>E:break\n				G=2+F._read_bits(D,B,2);B+=2\n			elif I==2:\n				if B+3+8>E:break\n				G=6+F._read_bits(D,B,3);B+=3\n			else:\n				if B+2+8+8>E:break\n				if F._read_bits(D,B,2)!=3:return\n				B+=2;G=13+F._read_bits(D,B,8);B+=8\n			if B+8>E:break\n			N=F._read_bits(D,B,8);B+=8;J.extend([N]*G)\n		for H in C(B,E):\n			if D[H]!=0:return\n		return J\n	def _build_transform_maps(A):A.fwd_transforms={};A.rev_transforms={}\n	def _build_pair_sequences(B):A=list(C(1,53));return[(B,C)for B in A for C in A]\n	def _apply_sequence(B,data,seq):\n		A=data\n		for C in seq:A=B.fwd_transforms[C](A)\n		return A\n	def _reverse_sequence(B,data,seq):\n		A=data\n		for C in f(seq):A=B.rev_transforms[C](A)\n		return A\n	def _compress_backend(E,data,safe=E):\n		C=data;B=[]\n		if J is not F:\n			try:\n				if safe:B.append((Z,Z+J.compress(C)))\n				else:B.append((b'L',J.compress(C)))\n			except:pass\n		if O:\n			try:\n				if safe:B.append((S,S+b.compress(C)))\n				else:B.append((S,b.compress(C)))\n			except:pass\n		B.append((T,T+C))\n		if not B:return T+C\n		G,D=W(B,key=lambda x:A(x[1]));return D\n	def _decompress_backend(E,data,safe=E):\n		B=data\n		if A(B)==0:return\n		if safe:\n			C=B[0:1];D=B[1:]\n			if C==T:return D\n			elif C==S and O:\n				try:return c.decompress(D)\n				except:pass\n			elif C==Z and J is not F:\n				try:return J.decompress(D)\n				except:pass\n			return\n		if O:\n			try:return c.decompress(B)\n			except:pass\n		if J is not F:\n			try:return J.decompress(B)\n			except:pass\n		if B[0]==ord('N'):return B[1:]\n	def _encode_marker_single(A,t):\n		if t<=252:return H([t-1])\n		return H([254,t-253])\n	def _encode_marker_raw(A):return H([252])\n	def _encode_marker_pair(B,t1,t2):A=(t1-1)*52+(t2-1);return H([253,A>>8&255,A&255])\n	def _decode_header(D,data):\n		B=data\n		if A(B)<1:return 0,()\n		C=B[0]\n		if C<252:return 1,(C+1,)\n		elif C==252:return 1,()\n		elif C==253:\n			if A(B)<3:return 0,()\n			E=B[1]<<8|B[2]\n			if E>=A(D.sequences):return 0,()\n			G,H=D.pair_lookup[E];return 3,(G,H)\n		elif C==254:\n			if A(B)<2:return 0,()\n			F=B[1]\n			if F>3:return 0,()\n			return 2,(253+F,)\n		else:return 0,()\n	def compress_with_best(E,data,safe=E,ultra=D):\n		N=ultra;J=safe;H=data\n		if not H:\n			L=E._compress_backend(I,J);Q=E._encode_marker_raw()+L\n			if not J:\n				O,U=E._decompress_auto(Q)\n				if O!=I:return E.compress_with_best(H,safe=D,ultra=N)\n			return Q\n		K=Y(h);M=F;V=E._compress_backend(H,J);G=E._encode_marker_raw()+V\n		if A(G)<K:K=A(G);M=G\n		for R in C(1,257):\n			try:\n				P=E.fwd_transforms[R](H);L=E._compress_backend(P,J);G=E._encode_marker_single(R)+L\n				if A(G)<K:K=A(G);M=G\n			except:continue\n		if N:\n			for(S,T)in E.sequences:\n				try:\n					P=E._apply_sequence(H,(S,T));L=E._compress_backend(P,J);G=E._encode_marker_pair(S,T)+L\n					if A(G)<K:K=A(G);M=G\n				except:continue\n		O,U=E._decompress_auto(M)\n		if O!=H:\n			if not J:B('Note: marker‑free mode produced ambiguous stream, falling back to safe markers...');return E.compress_with_best(H,safe=D,ultra=N)\n			else:raise RuntimeError('Safe compression failed – unexpected internal error!')\n		return M\n	def _decompress_auto(B,data):\n		G,C=B._decode_header(data)\n		if G==0:return I,F\n		H=data[G:];A=B._decompress_backend(H,safe=D)\n		if A is F:A=B._decompress_backend(H,safe=E)\n		if A is F:return I,F\n		try:\n			if not C:J=A\n			else:J=B._reverse_sequence(A,C)\n		except:return I,F\n		return J,C\n	def full_self_test(G):\n		B('='*60);B('PAQJP 8.8 – FULL SELF‑TEST (100% lossless)');B('='*60);J=D;B('Testing all 256 single transforms on all 256 byte values...')\n		for K in C(1,257):\n			for L in C(256):\n				O=H([L])\n				try:\n					S=G.fwd_transforms[K](O);T=G.rev_transforms[K](S)\n					if T!=O:B(f"  FAIL: transform {K} on byte {L:02x}");J=E;break\n				except N as V:B(f"  FAIL: transform {K} on byte {L:02x} raised {V}");J=E;break\n			else:\n				if K%32==0 or K==256:B(f"  PASS: transforms 1..{K} OK on all bytes")\n			if not J:break\n		if not J:B('\\n[FAIL] Single‑transform test failed.');return E\n		B(f"\\nTesting all {A(G.sequences)} transform pairs on all 256 byte values...")\n		for(Y,M)in e(G.sequences):\n			for L in C(256):\n				O=H([L])\n				try:\n					S=G._apply_sequence(O,M);T=G._reverse_sequence(S,M)\n					if T!=O:B(f"  FAIL: pair {M} on byte {L:02x}");J=E;break\n				except N as V:B(f"  FAIL: pair {M} on byte {L:02x} raised {V}");J=E;break\n			if not J:break\n			if(Y+1)%256==0:B(f"  PASS: {Y+1} pairs tested on all bytes")\n		if not J:B('\\n[FAIL] Pair test failed.');return E\n		B('  PASS: all pairs OK on all bytes');B('\\nTesting random 1000‑byte block through full compress/decompress...');a=U.Random(12345);P=H(a.randint(0,255)for A in C(1000))\n		for(W,Q)in[('marker‑free',E),('safe',D)]:\n			X=G.compress_with_best(P,safe=Q,ultra=D);b,Z=G._decompress_auto(X)\n			if b!=P:\n				B(f"  Retrying with forced safe markers...");c,M=G._decode_header(X);d=X[c:];R=G._decompress_backend(d,safe=D)\n				if R is not F:\n					if M:R=G._reverse_sequence(R,M)\n					if R==P:B(f"  PASS after retry with safe markers in {W} mode");continue\n				f=G.compress_with_best(P,safe=D,ultra=D);g,Z=G._decompress_auto(f)\n				if g!=P:B(f"  FAIL: random data pipeline mismatch in {W} mode after all retries");return E\n				else:B(f"  PASS after re‑compression with safe mode in {W} mode")\n			else:0\n		B('  PASS: random data pipeline OK in both modes');B('\\nTesting empty input...')\n		for Q in[E,D]:\n			h=G.compress_with_best(I,Q);i,Z=G._decompress_auto(h)\n			if i!=I:B(f"  FAIL: empty input pipeline mismatch (safe={Q})");return E\n		B('  PASS: empty input pipeline OK');B('\\n[All tests passed – compressor is 100% lossless]');return D\n	def compress_file(I,infile,outfile,ultra=D):\n		F=outfile\n		try:\n			with R(infile,'rb')as C:G=C.read()\n		except N as D:B(f"Error reading file: {D}");return\n		H=I.compress_with_best(G,safe=E,ultra=ultra)\n		try:\n			with R(F,'wb')as C:C.write(H)\n		except N as D:B(f"Error writing output file: {D}");return\n		B(f"Compressed {A(G)} → {A(H)} bytes → {F}")\n	def decompress_file(J,infile,outfile):\n		H=outfile\n		try:\n			with R(infile,'rb')as C:K=C.read()\n		except N as D:B(f"Error reading file: {D}");return\n		E,G=J._decompress_auto(K)\n		if E==I and G is F:B('Decompression failed.');return\n		try:\n			with R(H,'wb')as C:C.write(E)\n		except N as D:B(f"Error writing output file: {D}");return\n		L='raw'if not G else f"sequence {G}";B(f"Decompressed ({L}) → {H} ({A(E)} bytes)")\ndef l():\n	I='Output file: ';B(f"{i}");B('256 single transforms + 2704 transform‑pair sequences (100% lossless).')\n	if J is F and not O:B('Warning: No compression backend found. Data will be stored raw.')\n	C=k();G=M('\\n1) Compress   2) Decompress   3) Full self‑test\\n> ').strip()\n	if G=='1':A=M('Input file: ').strip();H=M(I).strip()or A+'.pjp';K=M('Choose mode:\\n  1) Fast     (256 transforms)\\n  2) Ultra    (256 + 2704 pairs)\\n> ').strip();L=D if K=='2'else E;C.compress_file(A,H,ultra=L)\n	elif G=='2':A=M('Compressed file: ').strip();H=M(I).strip()or A.rsplit(Q,1)[0]+'.orig';C.decompress_file(A,H)\n	elif G=='3':C.full_self_test()\n	else:B('Invalid choice.')\nif __name__=='__main__':l()\n""")
